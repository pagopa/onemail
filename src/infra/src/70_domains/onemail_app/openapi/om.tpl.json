{
  "openapi": "3.0.0",
  "info": {
    "title": "OneMail Dispatcher API",
    "version": "1.6.7",
    "description": "Service that manages the orchestration and dispatching of high-volume email queues.",
    "contact": {
      "email": "team-one-mail@pagopa.it"
    },
    "license": {
      "name": "Apache 2.0",
      "url": "https://www.apache.org/licenses/LICENSE-2.0.html"
    }
  },
  "servers": [
    {
      "url": "${server_url}"
    }
  ],
  "x-amazon-apigateway-api-key-source": "HEADER",
  "components": {
    "securitySchemes": {
      "api_key": {
        "type": "apiKey",
        "name": "x-api-key",
        "in": "header"
      }
    }
  },
  "paths": {
    "/v1/emails/statuses": {
      "get": {
        "tags": ["Emails"],
        "summary": "Get email status history",
        "security": [{ "api_key": [] }],
        "parameters": [
          {
            "name": "requestId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "x-amazon-apigateway-integration": {
          "type": "HTTP_PROXY",
          "httpMethod": "GET",
          "uri": "${uri}/v1/emails/statuses",
          "connectionType": "VPC_LINK",
          "connectionId": "${connection_id}",
          "passthroughBehavior": "WHEN_NO_TEMPLATES",
          "timeoutInMillis": 20000
        },
        "responses": {
          "200": { "description": "Email status history retrieved successfully" },
          "400": { "description": "Bad request - invalid data" },
          "401": { "description": "Unauthorized - authentication required or invalid credentials" },
          "403": { "description": "Forbidden - permission denied to perform the request" },
          "404": { "description": "Resource not found" },
          "409": { "description": "Conflict - resource already exists" },
          "429": { "description": "Too many requests" },
          "500": { "description": "Generic error" }
        }
      }
    },
    "/v1/emails/send/high": {
      "post": {
        "tags": ["Emails"],
        "summary": "Send high priority single email",
        "security": [{ "api_key": [] }],
        "parameters": [
          %{ if env != "prod" }
            {
              "name": "dryRun",
              "in": "query",
              "required": false,
              "schema": {
                "type": "boolean",
                "default": false
              }
            }
          %{ endif }
        ],
        "x-amazon-apigateway-integration": {
          "type": "HTTP",
          "httpMethod": "POST",
          "uri": "${uri}/v1/emails/send/high",
          "connectionType": "VPC_LINK",
          "connectionId": "${connection_id}",
          %{ if env != "prod" }
          "requestParameters": {
            "integration.request.querystring.dryRun": "method.request.querystring.dryRun"
          },
          %{ endif }
          "requestTemplates": {
            "application/json": ${tenant_request_template}
          },
          "passthroughBehavior": "WHEN_NO_MATCH",
          "timeoutInMillis": 20000,
          "responses": {
            "4\\d{2}": { "statusCode": "400" },
            "5\\d{2}": { "statusCode": "500" },
            "202": { "statusCode": "202" },
            "400": { "statusCode": "400" },
            "401": { "statusCode": "401" },
            "403": { "statusCode": "403" },
            "404": { "statusCode": "404" },
            "409": { "statusCode": "409" },
            "429": { "statusCode": "429" },
            "500": { "statusCode": "500" }
          }
        },
        "responses": {
          "202": { "description": "Email accepted for processing" },
          "400": { "description": "Bad request - invalid data" },
          "401": { "description": "Unauthorized - authentication required or invalid credentials" },
          "403": { "description": "Forbidden - permission denied to perform the request" },
          "404": { "description": "Resource not found" },
          "409": { "description": "Conflict - resource already exists" },
          "429": { "description": "Too many requests" },
          "500": { "description": "Generic error" }
        }
      }
    },
    "/v1/emails/send/low": {
      "post": {
        "tags": ["Emails"],
        "summary": "Send low priority email with single or multiple recipients",
        "security": [{ "api_key": [] }],
        "parameters": [
          %{ if env != "prod" }
            {
              "name": "dryRun",
              "in": "query",
              "required": false,
              "schema": {
                "type": "boolean",
                "default": false
              }
            }
          %{ endif }
        ],
        "x-amazon-apigateway-integration": {
          "type": "HTTP",
          "httpMethod": "POST",
          "uri": "${uri}/v1/emails/send/low",
          "connectionType": "VPC_LINK",
          "connectionId": "${connection_id}",
          %{ if env != "prod" }
          "requestParameters": {
            "integration.request.querystring.dryRun": "method.request.querystring.dryRun"
          },
          %{ endif }
          "requestTemplates": {
            "application/json": ${tenant_request_template}
          },
          "passthroughBehavior": "WHEN_NO_MATCH",
          "timeoutInMillis": 20000,
          "responses": {
            "4\\d{2}": { "statusCode": "400" },
            "5\\d{2}": { "statusCode": "500" },
            "202": { "statusCode": "202" },
            "400": { "statusCode": "400" },
            "401": { "statusCode": "401" },
            "403": { "statusCode": "403" },
            "404": { "statusCode": "404" },
            "409": { "statusCode": "409" },
            "429": { "statusCode": "429" },
            "500": { "statusCode": "500" }
          }
        },
        "responses": {
          "202": { "description": "Email accepted for processing" },
          "400": { "description": "Bad request - invalid data" },
          "401": { "description": "Unauthorized - authentication required or invalid credentials" },
          "403": { "description": "Forbidden - permission denied to perform the request" },
          "404": { "description": "Resource not found" },
          "409": { "description": "Conflict - resource already exists" },
          "429": { "description": "Too many requests" },
          "500": { "description": "Generic error" }
        }
      }
    },
    %{ if env != "prod" }
      "/v1/emails/sanitize-html": {
        "post": {
          "tags": ["Emails"],
          "summary": "Test HTML content sanitization of the email, only available in non-production environments",
          "x-amazon-apigateway-integration": {
            "type": "HTTP_PROXY",
            "httpMethod": "POST",
            "uri": "${uri}/v1/emails/sanitize-html",
            "connectionType": "VPC_LINK",
            "connectionId": "${connection_id}",
            "passthroughBehavior": "WHEN_NO_TEMPLATES",
            "timeoutInMillis": 20000
          },
          "responses": {
            "200": { "description": "Email sanitized successfully" },
            "400": { "description": "Bad request - invalid data" },
            "401": { "description": "Unauthorized - authentication required or invalid credentials" },
            "403": { "description": "Forbidden - permission denied to perform the request" },
            "404": { "description": "Resource not found" },
            "409": { "description": "Conflict - resource already exists" },
            "429": { "description": "Too many requests" },
            "500": { "description": "Generic error" }
          }
        }
      },
    %{ endif }
    "/health/ready": {
      "get": {
        "tags": ["Health"],
        "summary": "Perform readiness check of the service",
        "x-amazon-apigateway-integration": {
          "type": "HTTP_PROXY",
          "httpMethod": "GET",
          "uri": "${uri}/health/ready",
          "connectionType": "VPC_LINK",
          "connectionId": "${connection_id}",
          "passthroughBehavior": "WHEN_NO_TEMPLATES",
          "timeoutInMillis": 5000
        },
        "responses": {
          "200": { "description": "Service is ready" },
          "400": { "description": "Bad request - invalid data" },
          "401": { "description": "Unauthorized - authentication required or invalid credentials" },
          "403": { "description": "Forbidden - permission denied to perform the request" },
          "404": { "description": "Resource not found" },
          "409": { "description": "Conflict - resource already exists" },
          "429": { "description": "Too many requests" },
          "500": { "description": "Generic error" }
        }
      }
    },
    "/health": {
      "get": {
        "tags": ["Health"],
        "summary": "Perform liveness check of the service",
        "x-amazon-apigateway-integration": {
          "type": "HTTP_PROXY",
          "httpMethod": "GET",
          "uri": "${uri}/health",
          "connectionType": "VPC_LINK",
          "connectionId": "${connection_id}",
          "passthroughBehavior": "WHEN_NO_TEMPLATES",
          "timeoutInMillis": 5000
        },
        "responses": {
          "200": { "description": "Service is alive" },
          "400": { "description": "Bad request - invalid data" },
          "401": { "description": "Unauthorized - authentication required or invalid credentials" },
          "403": { "description": "Forbidden - permission denied to perform the request" },
          "404": { "description": "Resource not found" },
          "409": { "description": "Conflict - resource already exists" },
          "429": { "description": "Too many requests" },
          "500": { "description": "Generic error" }
        }
      }
    }
  }
}
